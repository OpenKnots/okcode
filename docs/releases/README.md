# Release Notes Index

Release policy and the canonical release process now live in [`docs/release.md`](../release.md).

Use this directory for versioned release notes and asset manifests only:

- `docs/releases/vX.Y.Z.md`
- `docs/releases/vX.Y.Z/assets.md`

When release policy changes, update [`docs/release.md`](../release.md) instead of duplicating the workflow matrix here.
